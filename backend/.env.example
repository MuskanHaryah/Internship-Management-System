# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/internship-management
# For MongoDB Atlas (cloud), use format:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/internship-management

# JWT Secret (IMPORTANT: Change this to a strong random string in production!)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Admin Credentials (for seed script - initial admin user setup)
ADMIN_NAME=Admin User
ADMIN_EMAIL=admin@internship.com
ADMIN_PASSWORD=Admin@123
